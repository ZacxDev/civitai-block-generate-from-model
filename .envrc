use flake

# The flake reads the node major out of `.nvmrc`, so a bump there has to
# re-enter the shell. nix-direnv watches flake.nix/flake.lock on its own; it
# cannot know about a file the flake happens to read.
watch_file .nvmrc
